Categories :
    change post input
    add categories (static)
    add it to post input
    Show it in posts

Delete post :
    add a popup to re ask the user before deleting his post
    add a popup to modify the post
    add this feature